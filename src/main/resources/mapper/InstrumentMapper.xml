<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cumtb.rdmsspring.mapper.InstrumentMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.cumtb.rdmsspring.entity.Instrument">
        <id column="instrumentId" property="instrumentId" />
        <result column="number" property="number" />
        <result column="antennaNumber" property="antennaNumber" />
        <result column="antennaFrequency" property="antennaFrequency" />
        <result column="host" property="host" />
        <result column="timeWindow" property="timeWindow" />
        <result column="points" property="points" />
        <result column="channels" property="channels" />
        <result column="touchId" property="touchId" />
        <result column="wheelSpace" property="wheelSpace" />
        <result column="gridSpace" property="gridSpace" />
        <result column="imageSpace" property="imageSpace" />
        <result column="space" property="space" />
        <result column="detectionId" property="detectionId" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        instrumentId, number, antennaNumber, antennaFrequency, host, timeWindow, points, channels, touchId, wheelSpace, gridSpace, imageSpace, space, detectionId
    </sql>

</mapper>
